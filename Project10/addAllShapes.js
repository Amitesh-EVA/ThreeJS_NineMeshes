import * as THREE from 'three';
import { createFrameShape } from './createFrameShape';

export function addAllShapes(originX, originY, outerH1, outerWidth, outerHeight, designWidth, designHeight, beadW, beadH){
    const allShapes=[];
}